# Variables pour docker compose (prod)
# Copier en .env à la racine du projet avant: docker compose -f docker-compose.prod.yml up -d
# Ou exporter: export $(cat docker-compose.env.example | grep -v '^#' | xargs)

DATABASE_URL=postgresql://postgres:VOTRE_MOT_DE_PASSE@postgres:5432/afriwonder
JWT_SECRET=votre_secret_jwt_long_et_aleatoire_min_32_caracteres
JWT_REFRESH_SECRET=votre_refresh_secret_long_et_aleatoire
DOMAIN=afriwonder.com
DB_USER=postgres
DB_PASSWORD=VOTRE_MOT_DE_PASSE_POSTGRES
