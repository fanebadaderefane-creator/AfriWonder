/**
 * Fallback TypeScript — Metro résout `.native.ts` / `.web.ts` sur les bundles.
 */
export { tryEnterPictureInPicture, prepareVideoCallSystemPip } from './liveNativeExtras.native';
export type { PipEnterOptions } from './liveNativeExtras.types';
