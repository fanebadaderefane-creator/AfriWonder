/** Options PiP système Android (expo-pip). */
export type PipEnterOptions = {
  /** Pas d’Alert — fallback silencieux (appels DM : bandeau vert WhatsApp). */
  silent?: boolean;
  width?: number;
  height?: number;
  title?: string;
};
