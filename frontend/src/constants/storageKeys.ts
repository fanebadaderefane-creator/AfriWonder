/** Paramètres → « Lecture auto » (fil vertical). */
export const STORAGE_FEED_AUTOPLAY = 'aw_settings_feed_autoplay';

/** Thème app (`dark` | `light` | `system`) — lu par ThemeProvider si pas de `user.theme` (invité / après logout). */
export const STORAGE_APP_THEME = 'aw_settings_app_theme';
