# Même API que le mobile : backend FastAPI (dossier ../backend), port 8000, préfixe /api.
#
# Laisser vide en dev : le navigateur appelle /api et Vite proxifie vers http://127.0.0.1:8000.
# URL absolue du backend (optionnel, ex. si pas de proxy) :
# VITE_BACKEND_URL=http://127.0.0.1:8000
