# Copier ce fichier en playwright-credentials.local (même dossier) et renseigner les valeurs.
# playwright-credentials.local est ignoré par git — ne jamais committer de mots de passe.
#
# Optionnel : URL de l’app sous test (défaut http://localhost:5173)
# PLAYWRIGHT_BASE_URL=http://localhost:5173

E2E_STAGING_EMAIL=
E2E_STAGING_PASSWORD=

E2E_ADMIN_EMAIL=
E2E_ADMIN_PASSWORD=
