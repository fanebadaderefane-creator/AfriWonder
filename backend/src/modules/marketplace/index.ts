/**
 * Module Marketplace — frontière domaine.
 * Implémentation : `src/routes/orders.routes.ts`, marketplace / panier.
 * Ne pas importer depuis `../auth` (utiliser événements ou files dédiées si besoin).
 */
export const MARKETPLACE_MODULE = 'marketplace' as const;
export const MARKETPLACE_ROUTES_FILE = 'orders.routes.ts' as const;
