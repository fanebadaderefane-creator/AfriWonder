# Configuration Railway pour AfriWonder - Backend uniquement
# Ce fichier force Railway à utiliser le dossier backend

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.backend"

[deploy]
startCommand = "node dist/index.js"
healthcheckPath = "/health"
