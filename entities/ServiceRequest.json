{
  "name": "ServiceRequest",
  "type": "object",
  "properties": {
    "client_email": {
      "type": "string"
    },
    "client_name": {
      "type": "string"
    },
    "provider_profile_id": {
      "type": "string"
    },
    "provider_email": {
      "type": "string"
    },
    "description": {
      "type": "string",
      "description": "Description de la demande"
    },
    "status": {
      "type": "string",
      "enum": [
        "pending",
        "accepted",
        "declined",
        "completed"
      ],
      "default": "pending"
    }
  },
  "required": [
    "client_email",
    "provider_profile_id",
    "description"
  ]
}
